"""Bundled, read-only UI fonts (SIL OFL 1.1).

Inter (sans-serif) and JetBrains Mono (monospace) are loaded in place at
startup by :func:`insider_scanner.gui.theme.fonts.load_application_fonts`.
Unlike the editable seed files, these are never copied into the user data
directory — they are read-only package resources.
"""
